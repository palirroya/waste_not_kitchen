# Waste Not Kitchen

Project for COP4710

## Set up

The repository must be initialized in `/MAMP/htdocs` (not `/MAMP/htdocs/Waste_Not_Kitchen`).

## Notes

Be careful with reset_database.php, it resets the entire database, meaning everything is deleted.
